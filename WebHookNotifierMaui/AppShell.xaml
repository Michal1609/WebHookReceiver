<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WebHookNotifierMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WebHookNotifierMaui"
    xmlns:views="clr-namespace:WebHookNotifierMaui.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="WebHook Notifier">

    <FlyoutItem Title="Main" Icon="bell.png">
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="History">
        <ShellContent
            Title="Notification History"
            ContentTemplate="{DataTemplate views:HistoryPage}"
            Route="HistoryPage" />
    </FlyoutItem>

    <FlyoutItem Title="Settings">
        <ShellContent
            Title="Settings"
            ContentTemplate="{DataTemplate views:SettingsPage}"
            Route="SettingsPage" />
    </FlyoutItem>

</Shell>
